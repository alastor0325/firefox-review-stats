{"summary": "The style inspector was corrected so it matches CSS selectors more accurately in modern web pages, especially around ::part() and nested shadow DOM elements. That means DevTools can show which styles actually apply to a component more reliably when page code uses web components and shadow roots. It does not change page rendering itself, but it makes debugging custom styling much more trustworthy."}